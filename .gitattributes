# Node parses LF everywhere; a CRLF checkout of src/apps/mcp-server.mjs breaks vitest's transform (28.08.2026).
* text=auto eol=lf

# Legacy upgrade fixtures (DESIGN-SPEC P0): the fixtures must survive a
# checkout byte-identical — the byte-equality test compares them exactly, and
# text normalization would corrupt the SQLite file.
src/testdata/legacy-datadir/** -text
*.db binary
